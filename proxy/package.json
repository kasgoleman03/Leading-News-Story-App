{
  "name": "news-reader-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Thin Express proxy that forwards requests to TheNewsAPI and the Go scorer service. Exists only so the API key never reaches the browser.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
