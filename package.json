{
  "name": "news-reader",
  "version": "1.0.0",
  "private": true,
  "description": "Root package.json exists only for Vercel. The /api functions are plain CommonJS JavaScript (api/top-stories.js, api/search.js). The canonical reference for the scoring + summarizer algorithms is the Go code in /services. The client has its own package.json under /client.",
  "scripts": {
    "vercel:dev": "vercel dev",
    "vercel:deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
